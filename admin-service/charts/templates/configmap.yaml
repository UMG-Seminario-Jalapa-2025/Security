apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "admin-service.fullname" . }}-config
  labels:
    {{- include "admin-service.labels" . | nindent 4 }}
data:
{{- range $key, $value := .Values.envVars }}
  {{ $key }}: {{ $value }}
{{- end }}
